{
  "name": "@juninmd/digest-kit",
  "version": "0.2.1",
  "description": "Núcleo dos agentes de digest: RSS → SQLite → IA → Telegram. O agente vira um arquivo de tópico.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/juninmd/digest-kit.git"
  },
  "type": "module",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./core": {
      "types": "./dist/core.d.ts",
      "bun": "./src/core.ts",
      "default": "./dist/core.js"
    }
  },
  "files": ["src", "dist", "README.md"],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "example": "bun run examples/meu-agente/src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "prepare": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "node-cron": "^4.6.0",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.3"
  },
  "engines": {
    "bun": ">=1.1.0",
    "node": ">=20"
  }
}
